import TopBar from "../Layout/TopBar";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <header>
      <TopBar />
      <NavBar />
    </header>
  );
};

export default Header;
